{
    "scripts": {
        "build": "npm run build:esm && npm run build:cjs",
        "build:esm": "tsc -p tsconfig.esm.json",
        "build:cjs": "tsc -p tsconfig.cjs.json",
        "test": "node esm && node cjs"
    },
    "dependencies": {
        "acebase": "^1.27.0",
        "acebase-core": "file:../node_modules/acebase-core",
        "acebase-server": "^1.16.2"
    },
    "devDependencies": {
        "typescript": "^4.9.3"
    }
}
